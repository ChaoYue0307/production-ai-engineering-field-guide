# Optional provider keys for extending the labs beyond deterministic fake clients.
# The default labs and tests do not require external API access.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional cost attribution labels used by observability examples.
AI_FEATURE_NAME=learning-lab
AI_TENANT_ID=local-dev
